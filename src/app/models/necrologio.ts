export class Necrologio {
  image: string;
  name: string;
  place_date: string;

  constructor(image: string, name: string, place_date: string) {
    this.image = image;
    this.name = name;
    this.place_date = place_date;
  }
}
